import Game from './scripts/game.js';
const styles = require('./styles/index.scss');
document.addEventListener('DOMContentLoaded', () =>{
    const unit = require('./scripts/unit.js');
    const game = new Game();
})